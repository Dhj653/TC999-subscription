# 萤核-人脸视频分类外挂扩展 自测验收清单

> 对应需求《项目需求（完整最终版）》。逐条勾选验证。已通过自动化验证的项目标注【已验证】。

## 一、架构约束

| # | 验收项 | 验证方法 | 期望结果 | 结果 |
|---|--------|----------|----------|------|
| 1.1 | 不修改萤核原版业务代码/页面/接口 | 对比原版源码 diff | 除 sidebar.js 一条路由外无其他原版改动 | ☐ |
| 1.2 | 人脸/抽帧/聚类/压缩包/排队在独立 Python 进程 | 检查 face_service 进程 | 全部在 5002 端口服务内 | ☐ |
| 1.3 | 萤核仅新增侧边栏+独立页面，HTTP 调 5002 | 查看前端 fetch | 全部走 `http://127.0.0.1:5002` | ☐ |
| 1.4 | 复用原 SQLite，只新增表不新建库 | 检查建表语句 | `CREATE TABLE IF NOT EXISTS` 仅 3 张新表【已验证】 | ☐ |
| 1.5 | InsightFace 禁入 Node 端；禁用 Ollama 聚类 | 查 Node 端代码 | 无人脸/聚类逻辑 | ☐ |
| 1.6 | Vue 页面存放外部独立文件夹 | 查目录 | `frontend_extension/views`，未入 `src-frontend/views`【已验证】 | ☐ |
| 1.7 | 原版仅改 sidebar.js 增一条路由 | diff | 仅 1 处 import + 1 处合并【已验证】 | ☐ |
| 1.8 | 输出双模式 A/B | 测试两种 | 默认源目录内 / 自定义输出目录 | ☐ |
| 1.9 | 路径安全校验 | 调用受限路径 | 拒绝 `C:\Windows` 等非法路径【已验证】 | ☐ |

## 二、Python 外挂服务-人脸视频聚类

| # | 验收项 | 验证方法 | 期望结果 | 结果 |
|---|--------|----------|----------|------|
| 2.1 | InsightFace 本地模型，禁云端 | 检查 `.env` + 代码 | `DISABLE_MODEL_DOWNLOAD=true`，缺模型报错退出 | ☐ |
| 2.2 | 仅女性正脸；男/侧脸/模糊/遮挡丢弃 | 用混合视频测试 | 仅女性正脸进入聚类 | ☐ |
| 2.3 | 相似度阈值 .env + 前端滑块可调 | 滑块改值后扫描 | 阈值运行时生效【已验证】 | ☐ |
| 2.4 | ffmpeg 抽帧 + InsightFace 特征 + FAISS 聚类 | 查代码 | 三者链路完整 | ☐ |
| 2.5 | 强制串行，单视频处理完清缓存 | 查 `task_queue` | 全局单锁 + `gc.collect()` + 清缓存【已验证】 | ☐ |
| 2.6 | 进度入库，支持断点续跑 | 中断后重启 | 跳过已记录映射【已验证】 | ☐ |
| 2.7 | 自动移动受测试模式管控 | 测试模式扫描 | 仅虚拟结果不移动【已验证】 | ☐ |
| 2.8 | 同组解析出单一人名→自动改名 | 文件名带演员名 | 分组自动重命名为该名【已验证】 | ☐ |
| 2.9 | 同组多不同人名→冲突不合并不改名 | 文件名含两人名 | 标记 `name_conflict`，交人工【已验证】 | ☐ |
| 2.10 | 单视频≥2 有效女性正脸→多人分组 | 多人视频 | 归入【多人】，不拆分源文件 | ☐ |

## 三、补齐 FileNeatAI 缺失能力

| # | 验收项 | 验证方法 | 期望结果 | 结果 |
|---|--------|----------|----------|------|
| 3.1 | 内存解析 zip/7z 直读包内文件 | 含视频的压缩包 | 列出并读取条目，不完整解压【已验证】 | ☐ |
| 3.2 | 加密压缩包跳过并输出日志 | 加密包测试 | 返回 None，日志记录【已验证】 | ☐ |
| 3.3 | 测试预览模式默认开启 | 首次启动 | `TEST_PREVIEW_MODE=true`【已验证】 | ☐ |
| 3.4 | 移动/改名前存原始路径与原名 | 查表 | `original_video_path`/`original_group_name` 有值【已验证】 | ☐ |
| 3.5 | 一键回滚全部文件与分组名 | 移动后回滚 | 文件归位、分组名还原【已验证】 | ☐ |
| 3.6 | 人工纠错：合并/删除/重处理/改名 | 调接口 | 均可用【已验证】 | ☐ |

## 四、API 接口

| # | 接口 | 验证 | 结果 |
|---|------|------|------|
| 4.1 | POST /api/scan_folder | 启动任务，返回 task_id【已验证】 | ☐ |
| 4.2 | GET /api/task_status | 返回进度/队列/日志【已验证】 | ☐ |
| 4.3 | GET /api/person_groups | 返回分组+视频+状态标签【已验证】 | ☐ |
| 4.4 | POST /api/move_files | 预览/真实移动受控【已验证】 | ☐ |
| 4.5 | POST /api/merge_group | 合并分组【已验证】 | ☐ |
| 4.6 | POST /api/revert_all_files | 一键回滚【已验证】 | ☐ |
| 4.7 | POST /api/reprocess_single | 重新处理单视频 | ☐ |
| 4.8 | PUT /api/group_rename | 改名【已验证】 | ☐ |
| 4.9 | GET /api/group_extract_names | 提取人名列表+冲突标记【已验证】 | ☐ |

## 五、数据库新增 3 表

| 表 | 字段要点 | 验证 |
|----|----------|------|
| video_task | 队列/状态/进度/日志/输出路径 | 服务启动自动建表【已验证】 |
| face_person_group | 分组id/名称/状态标记/质心 | 同上【已验证】 |
| face_video_mapping | 视频-分组/原始路径/原名/来源 | 同上【已验证】 |

## 六、前端页面

| # | 验收项 | 结果 |
|---|--------|------|
| 6.1 | 侧边栏【人脸视频分类】菜单 | ☐ |
| 6.2 | 源文件夹选择 + 自定义输出勾选 | ☐ |
| 6.3 | 测试开关 + 相似度滑块 + 启停 + 进度条 + 实时日志 | ☐ |
| 6.4 | 分组列表含状态标签【自动编号/多名称冲突/多人】 | ☐ |
| 6.5 | 分组操作：人名/改名/合并/重处理/删除 | ☐ |
| 6.6 | 全局操作：预览/执行移动/一键回滚 | ☐ |
| 6.7 | HTTP 调 5002 | ☐ |

## 七、交付物

| # | 交付物 | 路径 | 结果 |
|---|--------|------|------|
| 7.1 | face_service 完整 Python 项目 + requirements.txt | `face_service/`【已验证】 | ☐ |
| 7.2 | 前端 Vue 页面 + 侧边栏配置（仅新增） | `frontend_extension/`【已验证】 | ☐ |
| 7.3 | Windows 一键启动脚本 | `scripts/start_all_windows.bat`【已验证】 | ☐ |
| 7.4 | 自测验收清单 | 本文件【已验证】 | ☐ |

## 八、禁止项核对

- [x] 不修改萤核原有 AI 分类/ollama/文件浏览逻辑
- [x] 不做多视频并发，强制串行【已验证：全局单锁】
- [x] 不把人脸模型部署到 Node 端
- [x] 同组多不同人名禁止自动合并/改名【已验证：name_conflict】
- [x] 男性/非正脸不参与聚类【代码：face_engine 过滤】
- [x] 多人视频不复制拆分源文件【代码：仅归入多人分组】
- [x] 有路径校验，禁止输出到危险/非法路径【已验证】
- [x] 单视频处理完清空人脸/帧缓存再处理下一个【已验证：gc + free_video_cache】

## 九、已自动化验证摘要（沙箱环境）

> 以下在 Linux 沙箱用合成数据/真实 SQLite 完成验证（ML 模型/ffmpeg 因体积未装，对应路径为代码级校验）：

1. 全部 Python 模块 `py_compile` 通过。
2. 数据库建表 + 增删改查 + 日志 + 质心 BLOB 读写通过。
3. 人名解析：`张三_高清.mp4→[张三]`、`[李四]xxx→[李四]`、`少女自拍4K→[]`（噪声过滤正确）。
4. 路径安全：拒绝空/非法字符/`C:\Windows`/`C:\Program Files`/穿越；允许 `C:\Users\me\Videos`。
5. 聚类：同人跨帧归一组、不同人新建组、质心=归一化均值、单名自动改名、多不同名→`name_conflict`回退原名、多人分组、质心持久化+从 DB 重建（供 reprocess）。
6. 压缩包：zip 列表/内存读取通过；加密读取走跳过逻辑。
7. API 端到端：health/config/scan_folder(校验)/person_groups/task_status/group_extract_names/group_rename/merge_group/move_files(预览+真实)/revert_all_files 全部通过；任务在缺 ML 依赖时优雅失败（不崩溃服务）。
8. 前端：Vue SFC `<script setup>` 与 sidebar JS `node --check` 通过。
9. patch_sidebar.py：数组字面量与变量导出两种形式均幂等注入，自动备份。
